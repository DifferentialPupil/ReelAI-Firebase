specVersion: "v1beta"
serviceId: "Firebase"
location: "us-west2"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "firebase-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
